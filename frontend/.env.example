# The default mode uses the real Phase 1 profile and monthly-record API.
# Set to demo only to inspect the existing sample-data UI.
NEXT_PUBLIC_DATA_MODE=live

# Server-only backend origin, without an /api suffix. Restart Next after changes.
BACKEND_API_URL=http://localhost:8080
